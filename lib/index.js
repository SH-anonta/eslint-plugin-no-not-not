/**
 * @fileoverview Don&#39;t allow using double not operator for converting a value to boolean
 * @author sofen hoque anonta
 */
"use strict";

var requireIndex = require("requireindex");

module.exports.rules = requireIndex(__dirname + "/rules");
